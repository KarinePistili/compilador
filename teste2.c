#include <stdio.h>

int main()
{
    int pera = 3;
    // char qualidade = 'A';
    float peso = 2.5 + 1;

    // printf("Existem %d peras de qualidade %c ", pera, qualidade);
    // printf("pesando %f quilos.\n", peso);

    if(pera != peso){
        printf("Pesado");
    }
}
